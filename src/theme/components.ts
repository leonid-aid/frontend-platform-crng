import { ComponentStyleConfig, StyleFunctionProps } from '@chakra-ui/react'

export const RTComponentStyles: Record<string, ComponentStyleConfig> = {
    Button: {
        baseStyle: {
            borderRadius: '100px',
        },
    },
    Select: {
        baseStyle: {
            borderRadius: '100px',
        },
    },
    Input: {
        baseStyle: {
            borderRadius: '100px',
        },
    },
}
